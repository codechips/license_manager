h1 #{title}
ul#licenses
  - each license in licenses
    li.license
      span.version #{license.version}
      span.product #{license.product}
      span.key #{license.key}
      a.destroy(href='/delete/#{license.key}') Radera

a(href='/new') Lägg till en ny licens
